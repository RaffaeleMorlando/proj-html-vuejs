// const { vue } = require("laravel-mix");

const app = new Vue(
  {
    el: '#app',
    data: {
      linkNavbar: ['Home','Courses','About Us','News','Pages','Contact','Purchase'],
    }
  }
);

console.log('hello');